# R74 — Session Mock Note

The current landing strength mock (`HeroPlanMockup`, `WorkbenchMockup`, `LogbookInsightsMockup`) is useful but **incomplete as a representation of a real training session**. It shows only the strength block.

A complete session may include warm-up, joint mobility, activation, coordination/balance, strength, conditioning, cooldown, breathing, mobility, and a short education note. See `mem/specs/session-structure-principles.md`.

Future mocks should rotate variants showing:

- general warm-up
- joint mobility
- activation
- coordination / balance
- strength (current)
- cardio / running / concurrent training
- cooldown
- client education

No UI change in R74. Tracked as part of Slice 4 (structured session blocks in generation output) and the mock-rotation backlog (`.lovable/r73-mock-audit.md`).